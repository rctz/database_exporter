# Example environment configuration for Database Exporter
# Copy this file to `.env` and adjust the values as needed.

# ---------------------------------------------------------------------------
# DATABASE CONNECTION SETTINGS
# ---------------------------------------------------------------------------
# Supported database type/driver (e.g. "mysql", "postgres")
DATABASE_TYPE=mysql

# Host or IP address where the database is running
DATABASE_URL=localhost

# Port on which the database service is listening
DATABASE_PORT=3306

# Name of the database/schema to connect to
DATABASE_NAME=my_database

# Credentials for authentication
DATABASE_USER=user
DATABASE_PW=password

# ---------------------------------------------------------------------------
# EXPORT CONFIGURATION
# ---------------------------------------------------------------------------
# Comma-separated list of tables to export. Example: "users,orders,products"
TABLE_NAME=robots,stations

# Prefix that will be prepended to every generated CSV filename.
# If left blank, only <table>_<timestamp>.csv will be used.
CSV_OUTPUT_PREFIX=export

# Directory where the CSV files will be stored. If left blank, a timestamped
# folder will be created in the current working directory.
OUTPUT_PATH= 